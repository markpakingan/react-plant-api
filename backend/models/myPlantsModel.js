const axios = require("axios");
const apiKey = process.env.API_KEY;

const db = require("../db");
const { NotFoundError } = require("../expressError");


const myPlantsModel {

    // Adds a plant to a PlantList
    static async addToMyPlantList() {
         
    }

}

module.exports = PlantListModel;